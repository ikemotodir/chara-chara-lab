> **このファイルはGitHubリポジトリ内にあります。USBでの受け渡しは不要です。**
> 作業PC・番人PCのどちらも `git pull` すれば最新版になります。
> 内容を更新したら push するだけで、もう一方のPCへ自動的に届きます。
> （AUTOPILOTの同期対象外なので、記事投稿で上書きされることはありません）
# 【引き継ぎ書】CHARA CHARA LAB サイト制作 → 作業PC用

**作成：2026年8月5日｜作成：番人PC側Claude Code → 宛先：作業PC側Claude Code**
**作業PCが担当するのは、次の3系統すべて。**

| | 対象 | 原本の場所 |
|---|---|---|
| **①** | CHARA CHARA LAB 本体サイト（トップ・記事一覧・記事ページ） | リポジトリの `site-src/` |
| **②** | ①直下の個別ページ（アニメーション制作など） | リポジトリの `site-src/` |
| **③** | パッチクリエイターズプログラム（3ページ） | リポジトリ直下 |

**作業前に §1 と §2 は必ず読むこと。**そこを読まずに触ると、書いたものが数時間後に消える。

---

## 0. 役割分担

| PC | 担当 |
|---|---|
| **作業PC**（このファイルの読み手） | **サイト制作の全部**（①②③） |
| **番人PC**（`C:\Claude\apps\chara-autopilot`） | **AUTOPILOTの開発と運用**（記事の自動生成・X投稿・サイトの自動公開） |

---

## 1. ⚠️ 最重要：なぜ「原本」の場所が2種類あるのか

CHARA CHARA LAB のサイトは、**一部が AUTOPILOT（番人PCの自動投稿システム）によって自動生成・自動公開されている。**

AUTOPILOT は記事を投稿するたび（1日2回）に、手元のテンプレートからHTMLを作り直して、GitHubへ**上書きPUT**する。対象は次のファイル：

```
index.html / articles.html / articles.json / service-*.html / posts/*.html / assets/直下
```

**つまり、これらをGitHub上で直接編集しても、次の記事投稿で無かったことになる。**
実際に2026年7月30日と8月4日、この事故が2回起きている。

### だから ①② は「テンプレート（`site-src/`）」を編集する

```
site-src/index_template.html    → index.html を生成
site-src/archive_template.html  → articles.html を生成
site-src/blog_template.html     → posts/*.html を生成（45本すべて）
site-src/service-anime.html     → service-anime.html にそのままコピー
```

`site-src/` は AUTOPILOT が絶対に上書きしない場所。ここが**①②の唯一の原本**。

### ③ は今までどおりリポジトリ直下を直接編集

```
creators.html / guideline.html / world.html / assets/creators/**
```

この4つは AUTOPILOT の同期対象リストに入っていないので、直接編集して push してよい（`lib/github.js` の `listBlogFiles()` を実行して確認済み・2026-08-05）。

### 🚫 絶対に直接編集してはいけないファイル（＝生成物）

```
index.html          articles.html      articles.json
service-anime.html  posts/*.html       assets/直下のファイル
```

これらは「出力」であって原本ではない。編集しても上書きされる。**必ず `site-src/` 側を直すこと。**

---

## 2. ①② の作業フロー（site-src 経由）

```
作業PC                          GitHub                     番人PC
  │                               │                          │
  │ site-src/*.html を編集         │                          │
  ├──────── git push ───────────▶ │                          │
  │                               │                          │
  │ ボスに「テンプレ更新した」と伝える                          │
  │                               │ ◀── pull_templates.bat ──┤ ボスが1クリック
  │                               │                          │ ↓ 検査→取り込み
  │                               │ ◀── 全ページ再生成＋反映 ─┤
  │                               │                          │
  │                        2〜3分後に本番反映                  │
```

### 作業PCがやること

1. `git pull`（**必須**。§2-3参照）
2. `site-src/` の中のテンプレートを編集
3. ローカルで見た目を確認する場合は、`{{ARTICLE_LIST}}` などのプレースホルダをダミーHTMLに置き換えたコピーで確認する（**原本のプレースホルダは消さない**）
4. `git add` → `git commit` → `git push`
5. **ボスに「テンプレートを更新したので、番人PCで取り込んでください」と伝える**

### 番人PCがやること（ボスが `pull_templates.bat` をダブルクリックするだけ）

1. GitHubの `site-src/` を取得
2. **必須プレースホルダが残っているか検査**（欠けていたら取り込みを中止して作業PCに差し戻し）
3. `template/` へ反映（取り込み前のファイルは `template_backup/` に退避）
4. 全ページを再生成 → GitHubへ同期

この安全装置は実際にテスト済み（2026-08-05）。`{{ARTICLE_LIST}}` を消したテンプレートを送ると、取り込みを拒否してローカルも無傷のままだった。

### 2-3. ⚠️ 作業開始前に必ず `git pull`

**作業PCのクローンは 2026年7月30日の状態で止まっている可能性が高い。**
7月31日〜8月5日に番人PCから直接更新した内容（§9）があるため、pullせずに編集すると巻き戻してしまう。

---

## 3. ① CHARA CHARA LAB 本体サイト

### 3-1. テンプレート仕様（消すとサイトが壊れるもの）

| ファイル | 生成先 | **消してはいけないプレースホルダ** |
|---|---|---|
| `index_template.html` | index.html（トップ） | `{{ARTICLE_LIST}}` … 最新6件のカードが入る |
| `archive_template.html` | articles.html（記事一覧） | `{{ARTICLE_LIST}}` … 全記事のカードが入る |
| `blog_template.html` | posts/*.html（記事45本） | `{{TITLE}}` `{{DATE}}` `{{PILLAR}}` `{{PILLAR_CLASS}}` `{{READ}}` `{{OG_IMAGE}}` `{{CONTENT}}` |
| `service-anime.html` | service-anime.html | なし（そのままコピーされる） |

各プレースホルダの意味：
- `{{TITLE}}` 記事タイトル ／ `{{DATE}}` 公開日（2026.07.31形式） ／ `{{CONTENT}}` 本文HTML
- `{{PILLAR}}` 4本柱の名前（実験ログ／設計理論／案件のリアル／パッチの世界）
- `{{PILLAR_CLASS}}` そのCSSクラス名（`log` / `theory` / `real` / `world`）
- `{{READ}}` 読了時間（分・自動計算）
- `{{OG_IMAGE}}` OGP画像の絶対URL（記事ごとの自動生成サムネが入る）

### 3-2. 触ってはいけない仕掛け

- **`<!-- GA-AUTOPILOT-START -->` 〜 `<!-- GA-AUTOPILOT-END -->`**
  Googleアナリティクスのタグを自動挿入する枠。**手で書かない・消さない。**生成時に毎回入れ替わる
- **記事カードのHTML構造**
  カードは AUTOPILOT 側（`lib/blog.js`）が組み立てて `{{ARTICLE_LIST}}` に差し込む。カードの中身のHTMLを変えたい場合は、テンプレートではなく番人PC側の変更が必要 → **§6で依頼**
  現在のカード構造：
  ```html
  <li class="card"><a href="posts/xxx.html">
    <div class="cover img log"><img src="assets/thumb_post_xxx.jpg"><span class="badge log">実験ログ</span></div>
    <div class="card-body"><h3>タイトル</h3><time>2026.07.31</time></div>
  </a></li>
  ```
  → **`.cover` `.cover.img` `.badge` `.card-body` のCSSは消さないこと**（消すとカードが崩れる）
- **サムネイル表示に必要なCSS**（2026-07-31に追加）
  ```css
  .cover.img{overflow:hidden}
  .cover.img img{width:100%;height:100%;object-fit:cover;display:block}
  ```

### 3-3. index_template.html の構造

セクションとID（ヘッダーナビのリンク先になっているので、IDを変えるならナビも直す）：

| セクション | ID | 内容 |
|---|---|---|
| `.hero` | `#top` | HERO（ピンク背景・パッチ画像） |
| `.stats` | — | 数字4つ（60体／3,000,000円／+550人／116人）※**事実なので改変禁止** |
| `.about` | `#about` | CHARA CHARA LABとは（モーダルを開くボタン） |
| `.articles` | `#articles` | 最新記事6件（`{{ARTICLE_LIST}}`） |
| `.atelier` | `#studio` | アトリエ写真グリッド |
| `.world` | `#world` | パッチの世界 |
| `.anim` | `#anim` | アニメーション帯（動画3本） |
| `.service` | `#service` | サービス4つ（クリックでモーダル） |
| `.program` | `#program` | **クリエイターズプログラムのバンド**（→ creators.html） |
| `.consult` | `#consult` | 相談CTA（X DM） |
| `.company` | — | 運営会社概要 |
| `footer .copyright` | — | © |

モーダル：`m-about` / `m-svc1`〜`m-svc4`（`data-modal="m-xxx"` を持つボタンで開く。`m-svc2` から service-anime.html へリンク）

ヘッダーナビ：ABOUT / BLOG / ATELIER / PATTI's WORLD / SERVICE / **CREATORS** / 相談する（pill）
※ ナビは `@media(max-width:960px)` で非表示になる（`nav a:not(.pill){display:none}`）。項目を増やすとPCで溢れるので注意

### 3-4. 画像を追加したいとき

- **新しい画像を足す場合**：リポジトリの `assets/` に**新しいファイル名で** push すればよい。AUTOPILOTはファイルを削除しないので、そのまま生き残る
- **既存の画像を差し替える場合**：番人PC側にも同名ファイルがあるため、テンプレート取り込み時に元に戻されることがある。**別名で追加してテンプレート側の参照を変える**のが安全（推奨）
- どうしても同名で差し替えたい場合は §6 で番人PCに依頼

---

## 4. ② アニメーション制作などの個別ページ

### 4-1. service-anime.html（現在ある唯一の個別ページ）

**URL**：https://ikemotodir.github.io/chara-chara-lab/service-anime.html
**原本**：`site-src/service-anime.html`（プレースホルダなし。そのままコピーされる）

構成：
- ヘッダー（①系フルナビ・追従）
- HERO「SERVICE 02 / アニメーション制作 / キャラクターは動かすと"資産"になる」＋モーダルボタン
- 制作事例（協力会社の実績への外部リンクカード4枚）
- 協力会社3社（えむとん／Amused Art Japan／samillia）
- CHARA CHARAの特徴 FEATURE 01〜04
- **参考になる記事カルーセル** ← ⚠️ 下記の仕掛けあり
- 連絡先CTA（池本さんの写真・X DM・メール）
- 運営会社概要＋©

### 4-2. ⚠️ カルーセルの自動更新機構（壊さないこと）

ページ末尾のJavaScriptが、公開後に `articles.json` を fetch して、**アニメーション関連の記事を自動で拾ってカルーセルに差し替えている。**

- キーワード：`アニメ|動画|映像|モーション|Live2D|3D|VFX|MV|動か`
- 該当記事が0件のときは、HTMLに直接書いてある静的カードのまま表示される（ローカルの `file://` で開いた時も静的カードのまま）
- **`#artTrack` というIDと `.art-card` の構造を変えると、この自動更新が壊れる**

### 4-3. 新しい個別ページを作る場合

**ファイル名を `service-` で始めること**（例：`service-design.html`）。
`site-src/` に置けば、番人PCの取り込み時に自動でコピー＆公開される（`service-*.html` というパターンで拾っている）。
`service-` で始まらない名前にすると同期対象外になり、公開されないので注意。

---

## 5. ③ パッチクリエイターズプログラム（3ページ）

### 5-1. これは何のためのページ群か（30秒版）

- 「おばけのパッチ」を**二次創作OKのキャラクターとして公式宣言**し、クリエイター（ボカロP・MV職人・イラストレーター等）に「このIPで創りたい」と思わせるためのページ群
- **戦略**：キャラクターの露出コストを自分以外（クリエイター・アルゴリズム）に払わせる構造を作る。初音ミクの「ピアプロ・ライセンス」に相当する**「許可の旗」**を立てるのが目的
- **AUTOPILOTがX投稿で撒いた種の着地点がこのページ**。直近は**ボカロP＋アニメーションスタジオとのMVプロジェクトの受け皿**になる
- だから creators.html は「読み物」ではなく**「創りたくさせる装置」**として設計する

### 5-2. 各ページの構成（2026-08-05 本番から実地抽出）

#### creators.html — プログラムのトップ
- HERO（PC2カラム）＋ CTA
- **「しくみは、この3つだけ。」** … ①創る・投稿する ②販売する ③いっしょに創る
- **「○△× 早見表」** … 連絡不要でOK／ひとこと相談を／NG の3段組
- **「あなたは、どのクリエイター？」** … 6分類の選択UI（`.pick` ボタン → 対応する `.panel` を表示。各パネルは「創る／投稿する／販売する／連絡がいるとき」＋Q&A＋ガイドライン全文リンク）
  1. 🎬 映像クリエイター・アニメーター
  2. 🖌️ イラストレーター・漫画家
  3. 🎵 音楽クリエイター・ボカロP
  4. 🧵 クラフト作家・ハンドメイド
  5. 📷 コスプレイヤー・写真／被写体
  6. ✒️ 小説家・その他の創作
- **資料室カード4枚** … キャラクター設定資料／おばけの街 No Life Town／原作絵本（無料公開中）／公式ストップモーション
- **招待制プログラムのCTA**（「もっと本気で創りたい人へ」）

#### guideline.html — 二次創作ガイドライン
冒頭宣言 ＋ 全9条 ＋ FAQ9問。**9条の文言は改変禁止**（§7-3）。

| 条 | 見出し |
|---|---|
| 第1条 | 権利について（著作権は池本将個人に帰属・合同会社スタジオパッチが委託管理。ここは強調表示） |
| 第2条 | 二次創作の対象 |
| 第3条 | 連絡なしでできること |
| 第4条 | 販売とロイヤリティ（累計200万円まで0%・超過分5%） |
| 第5条 | クレジットとハッシュタグ（#パッチワーク） |
| 第6条 | 禁止事項 |
| 第7条 | 公式による紹介 |
| 第8条 | 免責と変更 |
| 第9条 | 連絡先 |

#### world.html — 設定資料
- **設定資料3カード** … パッチ Patti the Spook／マユちゃん Mayu／おばけの街「No Life Town」
- **🎨 創作用素材キット（準備中）** ← タスクC（§5-4）
- **公式ストップモーション動画**（`assets/creators/stopmotion.mp4`・自動再生ミュートループ・poster付き）
- **絵本「おばけのパッチ」**（原作・無料公開中）
- 「公式も、実験中。」セクション

### 5-3. 画像素材

**使用中**：`assets/creators/` 配下（hero＝おばけ集合絵／profile＝パッチ表情集／mayu＝三面図／ehon＝絵本表紙／nolifetown.jpg／stopmotion.mp4＋poster）。全てグレースケールPNG化で軽量化済み。

**未使用素材**（作業PCの `Desktop\Work\☆CHARA CHARA\クリエイターズプログラム\` にある）：

| ファイル | 備考 |
|---|---|
| Family photo.jpg | 家族絵（Pappa／Patti／Tippi／Titti）。**掲載可否がボス保留中** |
| Patti the Spook.jpg | 単体立ち絵 |
| metaverse2.png | メタバース画像 |
| stop_motion_gif.gif | 100MB・**GitHubの上限超えで使用不可** |

### 5-4. タスクC：創作用素材キットの公開（次の主タスク候補）

world.html に「準備中」の表記が**2箇所残っている**（2026-08-05 実地確認）。これを実体化するのがタスクC。
**内容・点数はボスが決める。勝手に中身を作らない。**素材が来たら `assets/creators/` に置き、カード内のリンクを差し替える。

---

## 6. 番人PC側の変更が必要なとき（依頼の出し方）

次のものはテンプレート編集では変えられない。番人PCに依頼する。

- 記事カードのHTML構造そのもの（`{{ARTICLE_LIST}}` に差し込まれる部分）
- ブログ本文のHTML変換ルール（Markdown → HTML）
- サムネイル画像の生成仕様（帯の位置・フォント・色）
- 記事の4本柱分類ロジック
- 既存画像の同名差し替え

**依頼に必要な情報：** ①対象 ②変更前→変更後 ③（あれば）画像ファイル

---

## 7. 🎨 デザイン仕様

### 7-1. ⚠️ ①②系と③系で、フォントとカラーパレットが違う（2026-08-05 実測）

**これは既知の差分であり、勝手に統一しないこと。**統一するかどうかはボスの判断事項。

| | ①② LAB本体 | ③ クリエイターズ |
|---|---|---|
| 本文フォント | **Zen Kaku Gothic New** | **Noto Sans JP** |
| 見出しフォント | Zen Maru Gothic | Zen Maru Gothic |
| イエロー | `#FFD43B` | `#FFC629` |
| インク（文字色） | `#2b2b2b` | `#2B2430` |
| ティント（淡ピンク） | `#fbf3f6` | `#FCEEF3` |
| ピンク | `#D6336C` | `#D6336C`（共通） |

ブランドガイド（§7-3の6色ルール）に完全一致しているのは**③のほう**。①②は先に作られた別系統のパレットで動いている。

**①②を編集するときは、そのファイル内で使われている CSS 変数をそのまま使うこと**（`var(--pink)` など）。色を直書きしない。

①②で使われているCSS変数：
```
--pink:#D6336C  --yellow:#FFD43B  --ink:#2b2b2b  --sub:#5c5057  --muted:#a89aa0
--line:#f0e6ea  --tint:#fbf3f6
--log-bg:#fbe3ec  --log-border:#f5cdda      （実験ログ／パッチの世界のカード色）
--theory-bg:#fff7d6  --theory-ink:#8a6d00  --theory-border:#ffe8a0  （設計理論）
--real-bg:#f2f0f1  --real-border:#e5e0e2   （案件のリアル）
```

### 7-2. 画像・キャラクターの扱い

- **キャラクター「パッチ」等をCSS・SVG・コード・AIで描かない。本物のPNGのみ**
- `img{height:auto}` をグローバルに効かせてある。画像に width/height 属性を足す時はこれが無いと縦長に潰れる（教訓済み）
- creators.html は 1280×800 で HERO 全要素が収まる設計（検証済み）。この1画面設計を壊さない

### 7-3. 絶対に守るルール（違反するとボスに怒られる）

1. **社名の英語表記は「STUDIO PATTI」のみ。「PATCH」「STUDIO PATCH」は一切禁止**（コミットメッセージ含む全出力物）
2. **キャラクターをAIやコードで描かない**（§7-2）
3. **事実・数字の改変・創作禁止**：ガイドライン全9条の文言／設定資料の内容／累計200万円まで0%・超過分5%／クラファン840,833円・89人／2022年10月24日発売／法人番号6180003026597／トップの数字4つ（60体・3,000,000円・+550人・116人）。不明な情報は【要事実確認】と書いてボスに確認
4. **世界観・設定をAIが創作しない**。world.htmlに書いてある内容だけが公式
5. 表記：「キャラクター」（「キャラ」と省略禁止）／「マスコットキャラクター」（単体「マスコット」禁止）
6. **記事タイトルのラベルは【】形式**（「実験ログ：〜」ではなく「【実験ログ】〜」）。2026-08-05ボス決定
7. ブランド色6色（`#D6336C` / `#A61E4D` / `#7A1638` / `#FFC629` / `#2B2430` / `#FCEEF3`系）＋ Zen Maru Gothic ＋ Noto Sans JP ＝ **③系の基準**。①②は§7-1の別パレット
8. 「縫い目（ステッチ）」モチーフを消さない
9. `.bat`・`.vbs` は ASCII only・CRLF・no BOM・no chcp。PowerShellスクリプトは BOM+CRLF

### 7-4. 確定済みの意思決定（ボス承認済み・変更禁止）

| # | 論点 | 決定 |
|---|---|---|
| 1 | ハッシュタグ | **#パッチワーク** |
| 2 | 公開範囲 | ページは誰でも閲覧可。**プログラム参加だけ招待制** |
| 3 | 料率 | **累計売上200万円まで0%・超過分のみ5%**（Unreal Engineと同じ考え方、と説明してよい） |
| 4 | 設定資料 | まずパッチとマユちゃん。他のおばけ（ミーン・モゥ等）は順次追加 |
| 5 | マユちゃん | **二次創作の対象外（公式作品のみ）**。「創作者自身のオリジナル人間キャラクター×おばけの世界」はOK |
| 6 | 販売 | 招待制にしない。200万/5%は「太っ腹の宣言」＝マーケ装置 |
| 7 | クラファン | 「事前相談。伴走支援もできます」（CAMPFIRE公式パートナーを武器化） |
| 8 | 動画収益化 | パートナープログラム収益は**非商用扱い・連絡不要・累計売上に含めない** |
| 9 | 著作権 | **作者・池本将個人に帰属。合同会社スタジオパッチは委託管理**（第1条で強調表示） |

---

## 8. サイト全体の地図（2026-08-05 実地確認済み）

公開URL：https://ikemotodir.github.io/chara-chara-lab/
リポジトリ：`ikemotodir/chara-chara-lab`（GitHub Pages・`main` が公開ブランチ）

| 公開ページ | 原本 | 反映方法 |
|---|---|---|
| index.html（トップ） | `site-src/index_template.html` | push → 番人PCで取り込み |
| articles.html（記事一覧） | `site-src/archive_template.html` | push → 番人PCで取り込み |
| posts/*.html（45本） | `site-src/blog_template.html` | push → 番人PCで取り込み |
| service-anime.html | `site-src/service-anime.html` | push → 番人PCで取り込み |
| creators.html | **そのファイル自体** | push だけで反映 |
| guideline.html | **そのファイル自体** | push だけで反映 |
| world.html | **そのファイル自体** | push だけで反映 |
| assets/creators/** | **そのファイル自体** | push だけで反映 |
| assets/直下（74ファイル） | 番人PC `blog_output/assets/` | 新規追加はpushでOK（§3-4） |
| articles.json | AUTOPILOT が自動生成 | 触らない |

---

## 9. 現状（2026年8月5日時点）— 7/30以降に番人PCがやったこと

作業PCのクローンが7/30で止まっている場合、以下は**まだ見ていない変更**。

### ✅ ゲート開放（7/31完了）
トップページの「準備中」バンドを廃止し、クリエイターズプログラムを正式公開。**ステルス状態は終了し、トップから正式導線が通っている。**
- バッジ `COMING SOON` → `NOW OPEN`／見出し「パッチクリエイターズプログラム、はじまりました。」／リンク「くわしく見る →」→ `creators.html`
- ヘッダーナビに **CREATORS** を追加（nav gap 30→22px、ナビ非表示のブレイクポイント 860→960px に分離）
- SERVICE 02 モーダルの「⏳詳細ページ準備中」→ `service-anime.html` へのリンク化（＝アニメ制作ページも同時公開）

### ✅ ヘッダー／フッターの2系統統一（7/31完了）
- **①②系**＝追従フルナビ（ABOUT/BLOG/ATELIER/PATTI's WORLD/SERVICE/CREATORS/相談する）＋運営会社概要（商号・代表・法人番号・所在地・創業・資本金・問合せ先）＋©フッター
- **③系**＝3ページ共通ナビ＋`#パッチワーク`タグ＋現在ページのピンクハイライト（`aria-current="page"`）

### ✅ ブログのサムネイル自動生成（7/31〜8/5）
- 4本柱ごとのベース画像の黒半透明帯に、記事ごとの文言を白文字で描画（最大2行・均等割り・帯の上下センター）
- `**語**` で囲むとその部分だけひまわり色（`#FFC629`）
- トップ／記事一覧のカード画像と、各記事のOGP（X共有時の画像）に自動反映
- **これに伴い `.cover.img` のCSSを index/archive テンプレートに追加済み**（§3-2）

### ✅ 記事の4本柱への振り分け（7/31完了）
全記事に `pillar`（実験ログ／設計理論／案件のリアル／パッチの世界）を付与。一覧のバッジ・カード色・サムネのベース画像がこれで切り替わる。

### 📊 数字
公開記事 **45本** ／ assets/直下 74ファイル

---

## 10. 🔎 未解決・ボス判断待ち

### 10-1. 本番リポジトリに残っている迷子ファイル13個

サイトのどこからもリンクされていない、7月10日版の残骸がGitHubのルート直下に残っている。

**古い記事7本**（`20260710-*.html`）
| ファイル | 現行記事との関係 |
|---|---|
| 20260710-2.html | `posts/20260712-6q5ll.html` と同内容 |
| 20260710-6.html | `posts/20260712-pk3ly.html` と同内容 |
| 20260710-ai-3.html | `posts/20260712-kbcbn.html` と同内容 |
| 20260710-k76gn.html | `posts/20260712-6.html` と同内容 |
| 20260710-mpcyn.html | `posts/20260712-ai-3.html` と同内容 |
| 20260710-mt4ls.html | 現行に同内容なし（旧バージョン） |
| 20260710-uwekv.html | 現行に同内容なし（旧バージョン） |

**画像6枚**（ルート直下 `atelier_2.jpg` `atelier_3.jpg` `atelier_main.jpg` `atelier_wide.jpg` `patti.png` `patti_side.png`）
→ **名前と中身がズレている**（例：`atelier_main.jpg` という名前だが中身はパッチの画像）。サイトが参照しているのは `assets/` 配下の正しい方なので表示上の害はない。

**判断が必要な点**：5本は現行記事と重複コンテンツ（SEO上は削除が望ましい）だが、7月中旬にXでこれらのURLを共有していた場合、削除するとリンク切れになる。「全部削除」か「残してcanonical設定」かをボスに確認。

### 10-2. 以前からの保留事項
1. **HERO集合絵**の右下に「NFT Museum 2021」の文字が入ったまま（意図通りか未確認）
2. **家族イラスト**（Pappa/Patti/Tippi/Titti）を載せるか・どこに載せるか（設定資料に「家族」項目がないため保留。素材は §5-3）
3. **タスクC：創作用素材キット**の公開 → §5-4
4. **①②と③のデザイン統一をするか** → §7-1

### 10-3. 番人PC側のタスク（作業PCは着手しない・参考情報）
- **タスクD**：#パッチワーク 巡回 → 公式紹介ループ（着手前にボス承認＋X APIコスト試算必須。月$10上限）
- **タスクE**：X APIのリンク付き投稿の課金調査（リンク付きは1本$0.20の可能性。1日5本だと月$30規模で上限超過の恐れ）

---

## 11. 🚨 事故ったときの復旧

| 症状 | 原因 | 対処 |
|---|---|---|
| 直したはずのトップページが元に戻っている | 生成物（index.html等）を直接編集した（§1違反） | `site-src/` 側を直して push → 番人PCで取り込み |
| テンプレートを送ったのに反映されない | 番人PCでの取り込みがまだ | ボスに `pull_templates.bat` の実行を依頼 |
| 取り込みが「中止しました」と出た | 必須プレースホルダを消した | エラーに出ているプレースホルダを戻して再push |
| creators.html等の変更が消えた | 極めて考えにくい。`git push --force` か別PCからの上書きを疑う | `git reflog` / GitHubのcommit履歴から復元 |
| 記事カルーセルが動かない（アニメページ） | `#artTrack` / `.art-card` の構造を変えた（§4-2） | 構造を戻す |
| GitHubにpushできない | 認証切れ | ボスに確認。**番人PCの `config.json` にPATがあるが、中身は画面に出さない・コミットしない** |
| サイトに反映されない | GitHub Pagesのビルド待ち | 2〜3分待つ |

---

## 12. 技術メモ

- ③の3ページは静的HTML・単一ファイル完結（CSS/JS内包・外部依存はGoogle Fontsのみ）
- ③のページ内機構：クリエイター選択UI（`.pick` → `.panel`）／画像フォールバック（`.imgless`）／`prefers-reduced-motion` 対応／スクロールリビール（IntersectionObserver）／OGPメタ設定済み（og:image は `assets/patti_square.png`）
- AUTOPILOTのGitHub同期は Contents API の GET+PUT のみ（DELETEは記事削除機能のみ）。2026-08-03から**内容が同一のファイルはコミットをスキップ**する（git blob sha比較）
- `blog_output/` は番人PCの**生成物置き場**。リポジトリには存在しない
- 番人PCの正規インストールは **`C:\Claude\apps\chara-autopilot`**。OneDriveデスクトップ配下の `chara-autopilot_dev-backup-20260804` は封印済みの旧コピーで、**絶対に起動しない**（2026-08-04に二重起動事故を起こした当該フォルダ）

---

## 13. 📋 ボスへの報告フォーマット

**①完了条件の結果 ②変更ファイル ③ボスの必要アクション** の3項目。
- 作業中に質問で割り込まない。完了後にまとめて報告
- 自己検証・自己修正を済ませてから報告（「ボスをテスターにしない」）
- 重要な警告は報告の最初に。手動操作の依頼は「1クリック1行」形式で末尾に
- **①②を変更したら、報告の最後に必ず「番人PCで `pull_templates.bat` を実行してください」と書く**

---

## 14. 📝 作業後の申し送り欄（作業PC側Claudeが記入）

```
作業日：2026年8月4日（作業PC・引き継ぎ受領）
やったこと：git pullで7/31〜8/4の全変更を取得。引き継ぎ書の記載を実地検証
　（site-srcテンプレート4本のプレースホルダ完備／迷子ファイル13個の実在／ライブのゲート開放状態）
変更したファイル：
　□ site-src/（①②）→ 変更なし（取り込み依頼なし）
　□ creators/guideline/world（③）→ 変更なし（pull取得のみ）
確認結果（本番URL・スマホ確認）：トップにNOW OPEN・creators.htmlリンク2箇所・「準備中」残骸ゼロを確認
番人PCへの依頼事項：なし
未解決・気づき：
　・迷子ファイル13個はボス判断待ち。旧記事7本はルート直下の画像6枚を一切参照していないことを確認
　　（＝画像6枚は完全孤立。どの選択肢でも記事と独立に処理できる）
　・GAマーカーはsite-src側には存在せず生成時挿入方式（README記載どおり。テンプレ編集時に手で書かない）
```

---

## 15. 作業ログ 2026年8月5日（作業PC）

**やったこと**
1. 迷子ファイル13個を処理（旧記事7本→転送ページ化／孤立画像6枚→削除）※ボス承認済み
2. ブランドカラーを新定義に統一（メイン #d6336c ／ ひまわり #FCC800 ／ 白 #FFFFFF ／ 黒 #2b2b2b）。
   ①②③の全7ファイル。本文フォントは Noto Sans JP に統一（見出しは Zen Maru Gothic のまま）
3. アニメーション制作ページ：事例4枠を動画埋め込み化。えむとん（AM株式会社）を事例・協力会社の
   両方から削除。縦型動画も16:9枠に統一（全画面にするとスマホ全面に出る）。4枠目「映画劇中
   アニメーション」に samillia 表記を追加
4. ロゴを「あめちゃんポップまる」の画像に差し替え（assets/logo_ccl_pink.png / logo_ccl_white.png）
   - フォント自体は**Webフォント利用がライセンスで禁止**のため、ボスが Canva で書き出した PNG を使用
   - 支給素材が白文字なので、白背景のヘッダー用に**ブランドピンク版を生成**（形状は不変・色のみ変換）
   - パレット化して2枚計17KB（元82KB）。全7ファイルのヘッダー＋③3ページのフッターに適用
5. 10エージェント監査を実施し、確定した4件を修正
   （ロゴのスマホ横潰れ／index_template の「キャラ」省略／service-anime の meta description
   早期終了／未使用の Vimeo player.js 読み込み）

**変更したファイル**
- [x] site-src/（①②）→ index_template / archive_template / blog_template / service-anime の**4本すべて**
      → **番人PCで pull_templates.bat の実行が必要**
- [x] creators.html / guideline.html / world.html（③）→ push済みで本番反映完了
- [x] assets/logo_ccl_pink.png / assets/logo_ccl_white.png を新規追加（push済み）

**確認結果**
- ロゴ画像2枚がライブで HTTP 200 を返すことを確認
- ③3ページのヘッダー／フッターにロゴが入り、旧テキストロゴの残骸ゼロ
- 必須プレースホルダ7種・#artTrack・.cover.img CSS はすべて無傷

**番人PCへの依頼事項**
1. `pull_templates.bat` の実行（①②の4テンプレート更新分）
2. 記事サムネイル生成の強調色を `#FFC629` → `#FCC800` に変更（ボスが伝達済み）

**未解決・気づき**
- 支給ロゴは3行組み（CHARA / CHARA / LAB）なので、ヘッダーでは幅53px前後と小さめ。
  横1行版があればヘッダーの見栄えを上げられる（ボス判断待ち）
- 素材キットの中身案は「素材キット企画メモ_参考IP調査_20260805.md」に調査済み（着手はボス指示待ち）
- 作業PCのブラウザプレビューがファイル切り替えを反映しない不具合あり。
  実機確認はローカルHTTPサーバ経由かライブURLで行うこと
---

## 16. 【番人PCへの依頼】pull_templates.bat の自動化（ボス要望・2026-08-05）

**要望**：作業PCが `site-src/` を push するたびにボスが番人PCで `pull_templates.bat` を
手動実行している。この操作をなくしたい。

### 推奨案：AUTOPILOTの記事投稿ジョブの冒頭に「site-src自動取り込み」を組み込む

- AUTOPILOT は既に**1日2回**動いているので、常駐プロセスやタスクスケジューラの追加が不要
- 処理の流れ（記事投稿の**前**に実行）:
  1. `git fetch` 相当で GitHub の `site-src/` の最新を取得
  2. ローカルの `template/` と差分があるか確認。無ければ何もしない
  3. **既存の必須プレースホルダ検査をそのまま通す**（ここは絶対に外さない）
  4. 検査OK → `template_backup/` に退避してから `template/` へ反映
  5. 検査NG → 取り込みを中止し、ログに理由を残して**通常の記事投稿は続行**
     （テンプレが壊れていても投稿は止めない）
  6. 以降は通常どおり全ページ再生成 → GitHub同期
- **効果**：ボスの操作がゼロになる。反映は最大12時間以内（次の投稿タイミング）
- `pull_templates.bat` は**残す**。急いで反映したいときは今までどおり手で押せる

### もっと早く反映したい場合（オプション）

タスクスケジューラで15〜30分ごとに上記1〜6のうち1〜5だけを実行する案もある。
ただし常駐タスクが1つ増えるので、まずは上記の「投稿ジョブに相乗り」を推奨。

### 作業PC側の運用はどう変わるか

- 変更なし。今までどおり `site-src/` を編集して push するだけ
- 報告文の「番人PCで pull_templates.bat を実行してください」の一文が不要になる
  （自動化が完了したら、この引き継ぎ書の §13 の該当ルールも書き換えること）
---

## 17. 引き継ぎ書の受け渡しルール（2026-08-05 変更）

**USBでの手運びは廃止した。この `HANDOVER.md` がリポジトリ直下にあるので、git で同期される。**

- 作業PC：内容を更新したら push するだけ
- 番人PC：`git pull` すれば最新版が手に入る（§16の自動化が入れば pull も自動になる）
- AUTOPILOT の同期対象は `index.html` / `articles.html` / `articles.json` /
  `service-*.html` / `posts/*.html` / `assets/直下` のみ。**`HANDOVER.md` は対象外**なので、
  記事投稿で上書きされる心配はない
- ボスに「USBで移して」と依頼してはいけない。両PCともリポジトリを持っているので不要

### ボスの手作業をゼロにするのが最優先方針

ボスは「ボタンを押す」「ファイルを移す」といった手作業を増やしたくない。
新しい仕組みを提案するときは、**ボスの操作が増えないか**を必ず先に検証すること。
増えるなら、増えない方法を先に探すこと。