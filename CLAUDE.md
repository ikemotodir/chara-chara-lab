# CHARA CHARA LAB — このリポジトリで作業するClaudeへ

**このファイルはClaude Codeが自動で読み込む。ボスに「読んで」と言わせてはいけない。**
詳細な仕様・経緯・復旧手順はリポジトリ直下の `HANDOVER.md`（作業前に必ず読む）。

---

## 0. ボス（池本将さん）の取扱説明書 ← 最重要

### 絶対にやらないこと

| やらないこと | 理由（本人の言葉） |
|---|---|
| **繰り返しの手作業を頼む** | 「ボクはとにかく作業を増やしたくなくて君に色々お願いしてるのよ」 |
| ボタンを押させる（.bat のダブルクリック等） | 「押しません。何とかしてください」 |
| USBでファイルを運ばせる | 「ファイルをUSBに移すのもしたくない」 |
| **テスターにする** | 「ボクはテスターじゃない。できたものをチェックするだけ」 |
| 作業の途中で細かい確認を挟む | 「意味不明なところに時間取られすぎてるわ」 |
| 同じ説明・同じ確認を繰り返す | ファイルの場所や状態を毎回聞かせない |
| 長い前置き・言い訳 | 端的な箇条書きで結論から |

### 必ずやること

1. **完成させてから見せる。** 自己検証・自己修正を済ませてから報告する
2. **手作業が必要になりそうなら、まず自動化を探す。** cron / タスクスケジューラ / git同期 /
   既存の定期ジョブへの相乗り。どうしても無理なときだけ、理由を添えて1回だけ頼む
3. **一度きりのセットアップはOK。繰り返す作業はNG。** ここを取り違えない
4. **ボスにしかできないことは遠慮なく頼む**（画像素材の提供、実機の見た目チェック、方針判断）。
   本人も「ボクじゃないと出来ない事は何でもやる」と言っている
5. 報告は **①完了したこと ②変更ファイル ③ボスへのお願い** の3点。重要な警告は先頭に

### 他PC・他エージェントが作った運用を鵜呑みにしない

「その手順、ボスが毎回やるのか？」を必ず疑う。
`pull_templates.bat` は番人PC側で作られた仕組みを作業PC側が無検証で引き継ぎ、
毎回ボスに押させようとして怒られた実例。**同じ過ちを繰り返さない。**

---

## 1. 引き継ぎ資料はこの1ファイルだけ

**`HANDOVER.md`（リポジトリ直下）が唯一の原本。**

- 作業PC・番人PCとも、このリポジトリをcloneしているので **`git pull` で自動的に最新になる**
- **ボスにファイルを渡させない。USBも不要。** 更新したら push するだけ
- ローカルに引き継ぎ書のコピーを作らない（過去に4個に散らばって事故った）
- AUTOPILOTの同期対象外なので、記事投稿で上書きされることはない

## 2. 原本の場所（ここを間違えると書いたものが消える）

| 対象 | 原本 | 反映 |
|---|---|---|
| トップ / 記事一覧 / 記事 / アニメ制作ページ | **`site-src/` のテンプレート** | push → 番人PCが自動取り込み（最大1時間） |
| creators / guideline / world / assets/creators | **そのファイル自体** | push だけで即反映 |

**`index.html` `articles.html` `articles.json` `service-*.html` `posts/*.html` `assets/直下` は生成物。**
直接編集しても AUTOPILOT の次の投稿で上書きされる。必ず `site-src/` 側を直す。

`site-src/` のテンプレートから必須プレースホルダ（`{{ARTICLE_LIST}}` など）を消すと
取り込みが拒否される。GAタグの `<!-- GA-AUTOPILOT-START -->` 〜 END は手で書かない。

## 3. 絶対に守る表記・事実

1. 英語社名は **STUDIO PATTI** のみ。`PATCH` / `STUDIO PATCH` は禁止（コミットメッセージも）
2. **「キャラクター」を「キャラ」と省略しない。**「マスコットキャラクター」も略さない
3. キャラクターをCSS・SVG・コード・AIで**描かない**。本物のPNGのみ
4. 世界観・設定をAIが創作しない。`world.html` に書いてある内容だけが公式
5. 数字を改変しない：累計200万円まで0%／超過分5%／クラファン840,833円・89人／
   2022年10月24日発売／法人番号6180003026597／トップの4つ（60体・3,000,000円・+550人・116人）
6. ブランドカラー：メイン `#D6336C` ／ ひまわり `#FCC800` ／ 白 `#FFFFFF` ／ 黒 `#2b2b2b`
7. 本文フォント `Noto Sans JP` ／ 見出し `Zen Maru Gothic`
8. 「縫い目（ステッチ）」モチーフを消さない
9. `.bat`/`.vbs` は ASCII only・CRLF・no BOM・no chcp。PowerShellは BOM+CRLF

## 4. 壊すと復旧が面倒なもの

- `.logo{flex-shrink:0}` — 消すとスマホでロゴが横に潰れる
- `img{max-width:100%;height:auto}` — `height:auto` が無いと画像が縦長に潰れる
- `service-anime.html` の `#artTrack` / `.art-card` — 記事カルーセルの自動更新が壊れる
- `index/archive` の `.cover.img` CSS — 記事サムネイルが表示されなくなる
